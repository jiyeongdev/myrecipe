<configuration>
   <property name="CONSOLE_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %magenta([%thread]) %highlight([%-3level]) %logger{5} - %msg %n" />
   <property name="SQL_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} %magenta([%thread]) %highlight([%-3level]){yellow} %logger{5} - %msg %n" />

    <!-- Console appender 설정 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <Pattern>${CONSOLE_PATTERN}</Pattern>
        </encoder>
    </appender>

    <!-- SQL 로그에만 노란색 하이라이트 적용 -->
    <appender name="SQL_APPENDER" class="ch.qos.logback.core.ConsoleAppender">
        <withJansi>true</withJansi>
        <encoder>
            <Pattern>---------%n${SQL_PATTERN}%n---------%n</Pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

    <!-- SQL 로그에 대한 로거 설정 -->
    <logger name="org.hibernate.SQL" level="DEBUG" additive="false">
        <appender-ref ref="SQL_APPENDER"/>
    </logger>
</configuration>